Solve both freestanding timber windmills at the eastern boundary as a related pair. Approximate root ground feet are (1178,388) for the smaller western mill and (1242,370) for the taller eastern mill. Both have stout grey stone polygonal bases, tapered timber/plaster tower bodies with diagonal bracing, orange-brown projecting timber belts, small warm roof caps, and four dark grey slatted lattice sails attached by timber arms to a forward hub. Match the different heights, spacing, body taper, stone base footprint, sail angles and front-facing plane orientation. The rotor crosses are diagonally oriented in the reference. These are intricate volumetric assemblies, not crossed flat rectangles: build wood rails, cross slats, hubs/axles, wall/base thickness, and full back structure. Inspect the tower in four compass views and ensure rotor does not intersect roof or tower. The edge behind them is the finite diorama boundary; do not extend land or build the distant graphic background. Trees, road and lamps visible in the crop are context owned elsewhere. Own only the two mills and immediate structural bases/steps.

Immutable integration contract
- Your writable scope is only your own fractal directory, plus new child material directories if descending. No git. Do not edit parent/sibling modules, camera or inherited manifests.
- Camera ../../camera-contract.json is locked. Use ROOT pixel coordinates, not magnified crop coordinates. view.json maps the 6x crop. A ground anchor must be a FOOT, never a roof peak. Use ctx.pixelToWorld(rootX,rootY,height) to locate geometry; foundations begin near world Y=.06.
- Deliver component.js exporting synchronous build(ctx) => THREE.Group. ctx contains THREE,camera,contract,pixelToWorld,worldToPixel. Shared primitives can be imported from ../scene/common.js (THREE,mat,box,polygon,ribbon). Export only your assigned assets; overlap in target crops is context, not duplicate ownership.
- Roads/bridges belong to settlements parent; lamps belong to east-village; terrain, water, trees and rocks belong to terrain sibling; UI pins belong to scene root. Do not create them in your component. Add only very short doorstep paths if essential. Report road/bank/lamps attachment issues in account.md for integration.
- Supplied preview.js adds immutable district placeholders OUTSIDE your ownership, district lamps, parent roads and your component. Other districts and greenery are deliberately absent.
- First create evidence/. Render with ./.render-tools/node/bin/node runs/final-medieval-village/fractal/scene/render.cjs runs/final-medieval-village/fractal/east-windmills/evidence/current-root.png '?settlements=../east-windmills/preview.js&no-ui'. Crop the root screenshot to crop_root_px and resize to target_size; compare side by side at matched magnification. Use ./.venv/bin/python for PIL utilities.
- Inspect one or two rotated views early, and all four compass directions before finishing: append &compass=0 through &compass=3. Current parent geometry is a provisional baseline, not a target. For final completion all your assets must have thickness, complete backs, attachments and ground contact. Recheck the locked reference view after completing hidden geometry.
- Identify the complete object/group and its calibrated faceted medieval-miniature style in two or three sentences in account.md. Record successive visual judgments and evidence files honestly. If internal structure/group layout warrants descent, prepare materials in sibling fractal/<child>/ directories, write children.json and END for runner dispatch; do not spawn agents. Otherwise write part.json only when complete, e.g. {"name":"east-windmills","module":"component.js","export":"build","children":[],"ground_y":0.06,"notes":[]}.
