Own only the larger two-story farmhouse with intersecting warm tiled roofs at root ground foot around (838,358). The reference has a long reddish brown main pitched roof running up-right, a smaller pale salmon gable projecting upper-left, another pale salmon triangular gable toward the right/front, ivory plaster panels and dark timber framing, roundish dark upper windows along the lit left side, and a recessed dark ground-floor opening. This is a joined multi-gable house, not three separate huts or one pyramid. Calibrate the footprint, wall height, main ridge and cross-gable attachment from the image; complete the clipped right face and hidden back plausibly in the same miniature style. Give all roof slopes eave thickness, real roof junctions, ridge caps and restrained tile relief; give all walls full volume and lower framing. Avoid large invented attachments. The ivory shed behind/left and blue mill in front/left are context owned by siblings; the lower thatched barn belongs to parent. Own no road, lamps, pins, greenery, or water.

Integration and evidence contract
- Write only inside your own fractal directory (and new child material directories if a genuine deeper subproblem requires descent). Do not use git, edit parent/sibling files, or change camera/reference/manifests. This work is approved; make reversible choices autonomously. Do not spawn agents: child solves are dispatched by the runner after children.json and ending your session.
- Deliver component.js with synchronous export function build(ctx) returning a THREE.Group. Shared primitives import from ../scene/common.js. ctx contains THREE, camera, contract, pixelToWorld(rootX,rootY,height), worldToPixel. Ground foundations begin at Y=.06. Use root image coordinates and feet, not magnified pixels or roof peaks, for anchors.
- Camera ../../camera-contract.json is locked. view.json describes crop_root_px and target_size. Render full root using ./.render-tools/node/bin/node runs/final-medieval-village/fractal/scene/render.cjs runs/final-medieval-village/fractal/east-tile-house/evidence/current-root.png '?settlements=../east-tile-house/preview.js&no-ui'. Then crop root image to crop_root_px and resize to target_size for matched comparison. Use ./.venv/bin/python and PIL to cut/resize evidence.
- Supplied preview.js includes parent lower barn, inherited placeholders outside your ownership, district lamps, and roads. Immutable parent snapshot is provisional context, NOT target geometry. Root terrain remains provisional and lacks greenery; report rather than model landscape residuals. Exclude all roads, lamps, natural features and UI pins.
- First identify the complete asset/group and its calibrated faceted medieval miniature style in 2–3 sentences in account.md. Render the whole crop, compare by eye, magnify conspicuous residuals; inspect 1–2 rotated views early. Repair simple differences locally; descend only for a real internal assembly/group requiring its own loop.
- Before completion inspect four compass directions (query &compass=0 through 3, or local centered audit camera without changing reference contract); roofs/walls/rotors/animals need actual thickness and full backs, attached members and ground contact. Recheck the locked crop after hidden-face repairs.
- Write part.json only after final completion, e.g. {"name":"east-tile-house","module":"component.js","export":"build","children":[],"ground_y":0.06,"notes":[]}. Save visual rounds and limitations in account.md. If descending, write child materials and children.json then END for runner dispatch without part.json.
