# Recursive Code World Models — project page

https://zhiqili-cg.github.io/RecursiveCWM/

Pages: Overview (index.html), Method (method.html), Explore a run (explore.html), Results (results.html). Two example runs (medieval village, city) are registered in data/scenes.json; add a run directory in the same layout and one entry there to add another. Static files only; serve locally with `python3 -m http.server 8000`. See PAGE.md for details.
