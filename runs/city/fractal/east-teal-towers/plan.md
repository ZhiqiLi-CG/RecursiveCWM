# east-teal-towers 执行方案

已批准工单；按本层目录限制执行，不使用 git，不再次请求批准。

- [x] 读取只读契约、接口、材料，并亲眼审阅同倍率基线。
- [x] generate.py 在根世界坐标生成三栋重复层叠塔、三棵分层绿树和灰紫地坪。输出 owned.json 与带父基线上下文的 preview.json。
- [x] 使用继承 render.mjs / scene.js 渲染 round0.png，与 target.png 等尺寸并排检查；按轮调整轮廓、层数、屋顶和遮挡。
- [x] 如有需要独立目标循环的子问题，备料并声明 children 后交运行器；否则本层直接完成重复结构。
- [x] 核验相机哈希、组件归属、唯一 ID、渲染错误和交付引用。最后写 account.md 与 part.json。

选择：直接用参数化真实几何实现这一重复组。各楼独立递归会分散重复节奏和遮挡的校准；此轮没有需要单独求解的子部件。视觉验收优先，数值仅记录。
