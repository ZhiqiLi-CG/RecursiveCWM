# 红树原型执行计划

采用已批准的本层工单，直接本地执行；不使用 git、不启动代理。

- 封闭树冠：前后两层真实 x-y 截面，以有厚度的三角形侧面连接。前轮廓四段锯齿，后轮廓圆钝且右边连续，左面亮红、侧面暗红。
- 树干：根落点 [226,487]，地面 y=0，双色棕色低边数柱体。
- 阴影：只围绕树干落点的小型水平多边形。
- 生成 generate.py、candidate.json 和包含只读父背景的 preview.json；用原 render.mjs/view.json 渲染。并排图片两边均 222×390。
- 亲眼复核与调整后保存 final.png、final-compare.png、account.md，最后写 part.json。检查相机摘要、唯一 ID、非空组件、只包含自身几何及原型锚点。
