# foreground-park 执行计划

已批准工单；采用 brainstorming 的范围拆解和 writing-plans 的步骤记录，不重复审批、不使用 git。

设计：三类树原型分别求解冠层形状与材质，两池组成一个湖景对象组。父层只负责树实例的布局、比例、遮挡与湖树关系。比所有对象留在一层更容易独立眼审；比每棵树单独求解更能保持重复形态一致。所有场景均使用继承的锁定相机和根世界坐标。

- [x] 读取 target/view/manifest/interface，相机只读；亲眼检查554×438目标与基线同倍率并排。
- [x] 记录四棵红树、两棵锥层绿树、五棵横展绿树的暂定地面锚点。
- [x] 从本层target裁切并放大四个子目标，复合回根图view；备齐brief/manifest/渲染入口与父基线上下文。
- [x] 渲染各子取景基线，检查尺寸、相机哈希、无浏览器错误；生成同倍率并排。子并排查看因/tmp空间不足失败，留在子循环补验。
- [x] 写children.json，交回运行器；结束当前会话。
- [x] 唤醒后读取四个part，校验ID/相机/归属，平移复制树原型并扁平合并双池，只含本层对象。
- [x] 渲染本层整体并排，调整重复对象比例、落地点及水池遮挡；需要再下钻则再次交回。
- [x] 视觉验收后写part.json（children与child_refs标明已经展平）和account.md。

收官：四子节点接回，integrated0/1实际渲染并排眼审通过，validation.json记录接口验证；最终交付part.json。
