# school-main-building 执行设计与计划

沿用相机契约及根世界坐标。使用实体挤出 L 形楼体、左侧矮翼、逐层檐口与青色窗面；不生成父层校地或树木。招牌若需独立循环，留出屋顶承接口并交运行器。

- [x] 渲染父快照 baseline，并与 target 同倍率并排眼审。
- [x] 写 generate.py：主体、翼楼、屋顶、檐口与窗框；输出 own.json 和 preview.json。
- [x] 每轮渲染 678×720，生成 compare，眼审轮廓、楼层关系和颜色。
- [x] 如果招牌需要独立求解，准备 school-sign 子节点目标、相机裁框、接口及只读上下文，写 children.json 后交回。
- [x] 孩子完成后整合，校验组件 ID、相机摘要与引用，再写 part.json 与 account.md。

用户已批准直接执行与可逆选择；本节点内执行，不使用 git，不自行启动代理。验证使用实际渲染和 JSON 接口校验，不新增实现镜像测试。
