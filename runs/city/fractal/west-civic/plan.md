# west-civic 递归执行计划

目标：在锁定根相机下重建西侧街区；本轮准备四个独立子问题，交回运行器。

设计：办公楼群、警局、三栋重复住宅、商店分别求解，包含各自宅地和附属细节。本层负责返回后的相互位置、遮挡和地坪连续性。所有组件使用根世界坐标；父层街道仅作预览上下文。

- [x] 读取只读材料与接口，重新渲染 776×458 本层基线并亲眼检查同倍率并排。
- [x] 用 prepare_children.py 从本层 target.png 裁出四个目标，以根图四倍统一放大；记录根图取景、来源裁框和继承哈希。
- [x] 各子节点复制局部渲染器，渲染基线并检查尺寸、相机哈希、浏览器错误和裁图覆盖。
- [x] 写 account.md、children.json 后结束会话，交由运行器启动孩子。
- [x] 被叫醒后验证并接入孩子 part.json，只扁平合并一次，重新进行本层同倍率眼审。
- [x] 消除地坪衔接和对象关系残差，必要时再次下钻；通过眼审后才写最终 part.json。

禁止 git；不启动代理；不修改相机、已有 manifest 或兄弟节点。唯一新增域为 west-civic 与本层声明的四个孩子目录。已完成再整体和校验，最终 part.json 已交付。
