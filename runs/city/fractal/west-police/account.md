# west-police 本层轮次账

本层深度 2，沿用锁定正交相机。根裁框 [87,245,159,105]，target 与 render 均为 636×420（根图 4 倍）；每张 compare 仅加 24 像素标题，不独立缩放两侧。

## 眼审与修正

- baseline：亲眼查看 baseline-compare.png，父快照只有地面与道路，警局与广场缺失。
- round1：生成主楼、低附翼、双檐线、二层窗带、底层窗门、屋顶灰盘与 H、竖立 POLICE 招牌及广场。查看 round1-compare.png，识别附翼未前凸、底层左侧过宽、层高与窗色不匹配。
- round2：收窄主楼底层，把附翼向前延伸，调低中檐、调整窗带和停机坪位置及 H 尺寸。查看 round2-compare.png，主楼与附翼落地点关系改善，上部体块仍略宽、屋顶偏低。
- round3：收窄上部后缘、抬高屋顶，调整底层窗高并提亮二层窗格。查看 round3-compare.png，发现屋顶下方蓝色带过厚、檐线分配不当、附翼檐口偏高。
- round4：重新分配上檐与蓝色带高度，降低附翼檐口，缩短窗带玻璃并修正中檐。亲眼查看 round4-compare.png 后接受本层结构与空间关系，未发现需要独立目标循环的子对象。

## 交付与范围

- part.json：83 个自有几何组件，ID 均以 west-police/ 开头；children 与 child_refs 均为空。
- build_local.py：可复现的源生成器，local.json 保存本层组件，preview.json 只用于叠加父快照预览。
- POLICE 是竖直面上的线段三角形字形；H 是屋顶实际条形几何；停机坪为 64 边形圆盘。未使用目标图贴图。
- 广场仅包含警局相连区域，未制作住宅、办公楼、前景树或主道路标线。父快照的 258 个组件不进入 part。
- 最终眼审证据：round5-compare.png；最终渲染：round5.png；渲染记录：round5.render.json；结构检查：verification.json。

## 验证记录与剩余差异

最终预览渲染无浏览器错误，952 个三角形（含父上下文）。检查了同倍率尺寸、锁定相机 SHA256、manifest 内容、唯一自有 ID、有限坐标、正盒体尺寸、三角形索引，以及最终组件排除父上下文。

按眼审判断结构，不以数值分数替代视觉通过。仍存在参考图的柔和阴影、窗格细微色差和材质渐变未复现；本模型使用平面纯色几何。招牌是简化线段字形，保留 POLICE 的可读性。预览缺少相邻建筑、树和部分地块边界细节，由对应节点及父层最终合成处理。父道路与目标的细微边缘偏差也未在本层覆盖修补。

全程未使用 git。相机契约和 manifest 未改写；写入仅在本节点目录。

## 唤醒后再整体：round5

收到孩子完成通知后核对：运行器已将原 children.json 移为 children.json.prev，其内容为 []；part.json 的 children 和 child_refs 也为空，且没有子节点 view.json 声明 west-police 为父节点。本次实际没有孩子组件可接入，未将兄弟节点误作孩子合并。

从本层最终 part 与继承父快照重新组成 preview.json；父快照短哈希仍为 5a9576da3b70，与 manifest 一致。重新渲染 round5.png，并亲眼查看 round5-compare.png（两侧均 636×420、根图四倍）。主楼与前凸附翼的连接、双檐线层次、招牌与屋顶位置、建筑落地及右侧广场关系连续；接受现有本层组件。相邻建筑与道路细节仍遵循原归属，未越域修改。

复核无新增下钻需求，不生成新的 children.json 调度请求。重新写入 part.json，保留 83 个自有组件、空 children 与 child_refs。新渲染无错误，与 round4 像素一致；只读输入哈希复核通过。原有柔和阴影、细微材质和简化字形差异仍按上节记录。
