# east-pink-housing 执行设计与计划

已批准工单；本节点内直接执行，不使用 git，不更改相机、manifest 或邻居。

采用参考图中可见轮廓建立实有三维面片：两栋同型凹入入口住宅，奶黄墙、粉红交叉坡屋顶、凸起天窗、门窗；另建根世界坐标宅地、步道、两株红树和前侧灌木。参数化原型加独立偏移便于分别复核。与纯长方体原型相比此方案能保留入口凹口；当前形体简单且可同层完整眼审，无需预先拆孩子。

- [x] 阅读目标、取景、接口和只读继承版本；亲眼检查基线同倍率并排。
- [x] generate.py 输出本层 draft.json 与组合父上下文的 preview.json；先渲染完整形体。
- [x] 在 616×440 同倍率并排图眼审两栋房顶、天窗、墙体、树和宅地连续性；修正可见残差后复渲染。
- [x] 最后眼审完成后输出 part.json、children.json 与 account.md；检查组件唯一性、引用、相机摘要和渲染错误。

验证依靠实际渲染及几何交付检查，不为一次性资产增加镜像实现测试。裁剪中商店、紫顶房和主路仅为上下文，最终组件不包含父底图。
