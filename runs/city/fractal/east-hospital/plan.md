# east-hospital 执行计划

已批准工单；仅在本节点写入，不使用 git，不自行启动子代理。

- [x] 读取固定相机、接口和目标，查看同倍率基线。
- [x] 用 build.py 生成根世界坐标组件：右高左低奶黄主体、向左延伸的白招牌、内凹屋顶、红十字、红侧盒、浅蓝窗门及小地坪。
- [x] 用 render.mjs 渲染 preview.json，与 target.png 同为 680×492 并排亲眼检查，按显眼残差迭代。
- [x] 核验相机摘要、ID、范围、引用及渲染无错误；写 account.md，最后生成 part.json。

方案选择：使用独立盒体和面几何，便于父层直接组合。当前医院细节规模无需独立求解循环，children=[]。不使用目标贴图。邻居缺失在账本说明，由父层整合。
