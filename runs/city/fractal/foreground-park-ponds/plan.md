# 双池执行方案

已批准工单；只写本节点，不使用 git、不启动代理、不重新拟合相机。

- [x] 读取目标、父接口、锁定相机并查看同倍率基线。
- [x] generate.py：手工控制点生成两片略高于草地的圆角非对称水面；按可见水岸补全红树遮挡区。生成有高度和折面的深绿/黄绿叶簇。
- [x] 生成 draft.json 和仅供预览的 preview.json，运行继承的 render.mjs，输出 740×460 与目标同倍率并排。
- [x] 亲眼逐池检查、调整明显残差；验证 ID、组件归属、相机哈希和渲染错误。
- [x] 写 account.md、children.json=[]，最后将验收的组件落盘 part.json。

选择真实根世界 polygon/triangles；不用目标贴图。矩形草地和父树均不归本节点。叶片足够小，可在本层直接调整，不需要独立子循环。眼审作为验收依据，像素统计仅作记录。
