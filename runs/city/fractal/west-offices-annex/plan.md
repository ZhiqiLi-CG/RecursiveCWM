# west-offices-annex 本层执行方案

已批准工单，直接本地执行，不使用 git 或自行启动代理。

目标：固定根相机下重建出画附楼，与父层塔楼自然连接。
方法：先检验顶部收窄、下部展开的剖面体块；对照目标核对轮廓与遮挡，再添加实际几何围边、凹屋顶、窗与小门。备选普通箱体会露出过大屋顶；全靠平面补片不利于连接，先采用有体积的剖面体块。

- [x] 阅读目标、基线、只读契约和接口，亲眼审视同倍率并排。
- [x] generate.py 输出自有 candidate.json，和只读父快照合并为 preview.json。
- [x] 用提供的 render.mjs 渲染 round1.png，生成左右各336×592的并排图并亲眼审视。
- [x] 依据可见残差调整轮廓、屋顶与细节，保留各轮图和参数。
- [x] 验证相机/输入哈希、组件ID与写域，完成 account.md 和 part.json；若有独立子问题则先备料并结束。

最终组件仅包含 west-offices-annex/ 前缀几何。无目标贴图，无父层地坪、道路、塔楼副本。验证以相机哈希、JSON接口检查、WebGL报告和亲眼并排为依据，不为静态可逆几何新增软件单测。
