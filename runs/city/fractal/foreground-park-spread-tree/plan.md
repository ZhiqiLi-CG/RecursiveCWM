# 横展树执行计划

已批准的单树原型；全部写入本目录，不使用 git，不启动代理。

- [x] 阅读 brief、锁定相机、共享接口并亲眼查看目标/基线同倍率对照。
- [x] generate.py 生成短棕树干、六组错位的厚实有机圆盘冠层与小型地面阴影。采用三维封闭环网格；不用目标贴图或面向相机的面片。
- [x] preview.json 只为预览合入父 scene/base.json；实际渲染 240×252 并生成同倍率 roundN-compare.png，按目视残差调整轮廓、位置、厚度与颜色。
- [x] 验证相机摘要、唯一组件 ID、单棵所有权、锚点及渲染无错误，写 account.md、children.json=[]，最后落盘 part.json。

选择：简单椭球不能表现清晰厚边；规则圆柱过于机械。采用封闭多环网格，横向冠层轻度不规则、顶面微拱、边缘圆滑，保留几何厚度。此规模可在本层完整求解，无需下钻。
