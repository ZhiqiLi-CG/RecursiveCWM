# east-district — depth 1 preparation plan

Approved execution: recursive reconstruction, reversible decisions autonomous, no git, runner dispatch only.

Design: five children with independent object silhouettes and material/detail loops. Shared root roads remain root-owned. Each child owns its immediate paving/plot and attached vegetation; overlapping crops convey context only. Repeated houses and towers are grouped so spacing, common geometry and variation can be reviewed together. No final part is emitted before returned components are assembled and visually reviewed.

- [x] Read inherited view, immutable manifest, locked camera, scene interface and parent substrate.
- [x] Personally inspect target and equal-scale baseline; all district objects are absent.
- [x] Prepare east-shop, east-pink-housing, east-purple-housing, east-teal-towers, east-hospital from this node's target; 4× root scale, 2× parent scale.
- [x] Record root-coordinate views, immutable inherited manifests, explicit ownership and local renderers. Context is a read-only snapshot of parent substrate, excluded from final components.
- [x] Render each child baseline with locked camera, assemble same-size comparisons, personally review coverage.
- [x] Verify image sizes, crop composition, hashes and zero renderer errors; publish children.json last and end session.
- [x] On runner resume, validate each returned part, compose flattened components once with child_refs, render this 1172×588 view, inspect target and render at equal scale and refine continuity.
- [x] Write final part.json only after integration review; account.md records evidence and remaining differences.

Files: prepare_children.py (reproducible preparation), context.json (preview only), plan.md, account.md, validation.json, children.json; descendants receive target.png, view.json, brief.md, manifest.json, render.mjs, scene.js and baseline evidence. Existing manifests and camera are never overwritten.
