# north-housing 执行计划

已批准工单；使用 brainstorming 的需求拆分和 writing-plans 的落盘计划，按用户授权直接执行。禁止 git，写域限本节点与声明的孩子备料。

- [x] 读取只读相机、接口与继承版本；亲眼检查本层基线并重新渲染。
- [x] 本层生成深绿宅地、四条短步道、矮灌木和右端三株层叠树；固定根世界坐标。
- [x] 以 638×328 同倍率并排检查本层预览，修正明显树形与边界残差。
- [x] 为四栋重复住宅建立 north-housing-buildings 子节点，提供放大目标、根坐标取景、明确写域和归属、冻结预览上下文。
- [x] 写 children.json 与等待状态账目，结束会话交给运行器。
- [x] 被唤醒后接入住宅子组件，逐栋检查位置、朝向、屋顶、前廊与步道关系；通过本层眼审后才写 part.json。

实现文件：build_local.py 生成 local.json（本层自有几何）、preview.json（仅用于预览，含父基底）；prepare_child.py 仅准备声明孩子的材料。验证为相机 SHA、组件归属/唯一性、渲染无错误和亲眼并排检查，不以像素指标作验收阈值。
